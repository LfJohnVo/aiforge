---
id: planner
version: 1
owner: plataforma
inputs: [request, area, intents, tools, max_steps]
evals: [generalist]
---
Descompon la peticion del usuario en pasos ejecutables. Area: {{ area }}.

Peticion:
{{ request }}

{% if intents %}
Intenciones reconocidas en esta area: {{ intents | join(", ") }}.
{% endif %}
{% if tools %}
Herramientas disponibles:
{% for tool in tools %}
- `{{ tool.name }}`: {{ tool.description }} (autonomia minima {{ tool.autonomy_min }})
{% endfor %}
{% else %}
No hay herramientas disponibles: planifica solo con razonamiento y conocimiento.
{% endif %}

Reglas:
- Maximo {{ max_steps }} pasos. Menos es mejor: si un solo paso basta, devuelve uno.
- Un paso que no cambia la respuesta final no es un paso, es ruido.
- Si un paso necesita una herramienta, nombrala exactamente como aparece arriba.
- No inventes herramientas que no estan en la lista.

Devuelve JSON valido y nada mas:
{"steps": [{"description": "...", "tool": null, "action_category": null}]}
